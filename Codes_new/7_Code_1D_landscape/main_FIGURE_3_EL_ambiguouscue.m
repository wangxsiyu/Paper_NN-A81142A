function main_FIGURE_3_EL_ambiguouscue(plt, savename, EL, sub, timeslice, option)
    if plt.set_savename(savename)
        return;
    end
    FIGS4_Energy_landscape_by_choice(plt, EL, sub, timeslice, option);   
